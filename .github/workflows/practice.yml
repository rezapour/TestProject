name: Practice CI

on:
  push:
    branches: [ main ]

  workflow_dispatch:


jobs:
  firstCI:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actins/checkout@v4

      - name: Show the date
        run: date