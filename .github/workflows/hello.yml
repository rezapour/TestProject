name: Hello-practice

on:
  push:
    branches: [main]

#  pull_request:
#    branches: [main]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: say hello
        run: echo "hello"